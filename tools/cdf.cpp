#pragma GCC optimize("O3")
#include <bits/stdc++.h>
using namespace std;

struct INIT {
  INIT() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    cout << fixed << setprecision(15);
  }
} __init;

int main() {
  int t;
  cin >> t;
  while (t--) {
  }
  return 0;
}